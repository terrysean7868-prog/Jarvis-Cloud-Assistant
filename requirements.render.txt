# Render (Linux/Cloud) minimal dependencies
# Keep this lean to improve build reliability on small instances.

# Web Framework and API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.25.0
aiohttp>=3.8.5
python-multipart>=0.0.6

# Authentication & Security
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0

# Database
pymongo>=4.4.0
aiofiles>=23.0.0

# LLM providers
openai>=1.0.0
google-generativeai>=0.3.0

# Speech-to-Text (cloud fallback for mobile browsers)
google-cloud-speech>=2.26.0

# Lightweight speaker biometrics (MFCC)
python_speech_features>=0.6

# Numpy is required by voice biometrics and many audio/math utilities.
# Use a modern version with wheels for Python 3.13 on Linux.
numpy>=2.1.0

# Version Control / SSH (optional features)
gitpython>=3.1.0
paramiko

# Background Job Scheduling
APScheduler>=3.10.0

# Web Scraping / Internet
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Utilities
psutil>=5.9.0

# Production server (optional)
gunicorn>=21.0.0
